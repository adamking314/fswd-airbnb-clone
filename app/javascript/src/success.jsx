import React from 'react';
import ReactDOM from 'react-dom';
import SuccessPage from './SuccessPage';  // Adjust the import as needed

ReactDOM.render(
  <React.StrictMode>
    <SuccessPage />
  </React.StrictMode>,
  document.getElementById('root')  // Ensure your 'root' div exists in your index.html
);